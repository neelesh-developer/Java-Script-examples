function multiplyNumbers(num1, num2, num3) {
    console.log("input recieved are " + num1 + " and " + num2 + " and " + num3);
    var result = num1 * num2;
    return result;
}
var x = multiplyNumbers(10, 2, 3);
multiplyNumbers(4, 5);
